# Portfolio

A personal portfolio website built around a real-time 3D neural network visualization. The camera sits inside a cylindrical network of neurons connected by thin filaments. Signals propagate along connections as gold light pulses. Scrolling moves through the cylinder.

## Running

```bash
npm install
npx vite --open
```
