/**
 * Projects Layer 2 — Passion Projects.
 * 3D group is empty; content is rendered via the HTML overlay (projectsOverlay.ts).
 */
import type { SectionDef } from '../sectionConfig';

export const projectsSection2: SectionDef = {
  id: 'projects-2',
  navId: 'projects',
  cellBodies: [],
  buildContent(_group) {
    // Content rendered via HTML overlay (projectsOverlay.ts)
  },
};
